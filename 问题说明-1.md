# HuggingFace 模型下载问题记录

## 问题描述

在使用 MedRAX 项目时，遇到 HuggingFace 模型下载失败的问题：
- 错误信息：`tls handshake eof` 和 `Request failed after 5 retries`
- 错误来源：`transfer.xethub.hf.co` (XetHub 存储服务)
- 即使配置了 `HF_ENDPOINT=https://hf-mirror.com` 和 VPN，问题依然存在

## 问题根本原因

### 1. XetHub 存储系统

**什么是 XetHub？**
- HuggingFace 在 2024 年收购了 XetHub，并将其技术集成到 Hugging Face Hub 中
- XetHub 是一个专门用于大文件传输的存储系统，用于提升大文件的传输效率和规模
- 某些模型（特别是大模型）会使用 XetHub 存储，而不是传统的 HuggingFace 存储

**为什么 XetHub 不受 `HF_ENDPOINT` 影响？**
- `HF_ENDPOINT` 环境变量主要用于指定 Hugging Face **API** 的基础 URL
- XetHub 使用独立的存储服务（`transfer.xethub.hf.co`），**不受 `HF_ENDPOINT` 控制**
- 即使设置了镜像站点，XetHub 的下载请求仍然会直接指向 `transfer.xethub.hf.co`
- 镜像站点（如 `hf-mirror.com`）通常只镜像传统的 HuggingFace 存储，不包含 XetHub 存储

### 2. VPN 和代理的问题

**为什么浏览器可以访问，但下载失败？**
- 浏览器可能使用了系统代理或 VPN 的全局代理
- Python 的 `transformers` 和 `huggingface_hub` 库可能：
  - 没有正确读取系统代理设置
  - 使用了独立的网络请求库（如 `reqwest`），绕过了系统代理
  - 需要显式配置代理环境变量

**TLS 握手失败的原因：**
- 网络不稳定导致连接中断
- 防火墙或代理服务器干扰了 TLS 握手过程
- XetHub 服务器在某些网络环境下连接不稳定

## 解决方案

### 方案 1：禁用 XetHub（推荐）

强制使用传统的 HuggingFace 下载方式，而不是 XetHub 存储：

```bash
# Windows PowerShell
$env:HF_HUB_DISABLE_XET="1"

# 或者在 .env 文件中添加
HF_HUB_DISABLE_XET=1
```

**注意：** 这个环境变量可能不是所有版本的 `huggingface_hub` 都支持，需要较新的版本。

### 方案 2：禁用 hf_transfer

`hf_transfer` 是 HuggingFace 的传输工具，在某些网络环境下可能有问题：

```bash
# Windows PowerShell
$env:HF_HUB_ENABLE_HF_TRANSFER="0"

# 或者在 .env 文件中添加
HF_HUB_ENABLE_HF_TRANSFER=0
```

### 方案 3：配置代理环境变量

即使使用了 VPN，也需要显式配置代理环境变量：

```bash
# Windows PowerShell
$env:HTTP_PROXY="http://your_proxy:port"
$env:HTTPS_PROXY="http://your_proxy:port"

# 或者在 .env 文件中添加
HTTP_PROXY=http://your_proxy:port
HTTPS_PROXY=http://your_proxy:port
```

### 方案 4：手动下载模型

如果自动下载一直失败，可以手动下载：

1. 访问模型页面：https://huggingface.co/StanfordAIMI/CheXagent-2-3b
2. 下载所有文件到本地
3. 将文件放置到正确的缓存目录：
   ```
   ./model-weights/models--StanfordAIMI--CheXagent-2-3b/snapshots/463999422d77fe01380ef03493e5ae3bb11bd004/
   ```

### 方案 5：暂时禁用有问题的工具

在 `main.py` 中注释掉使用 XetHub 存储的工具：

```python
selected_tools = [
    "ImageVisualizerTool",
    "DicomProcessorTool",
    "ChestXRayClassifierTool",
    "ChestXRaySegmentationTool",
    "ChestXRayReportGeneratorTool",
    # "XRayVQATool",  # 暂时注释掉，这个工具使用 XetHub 存储
]
```

## 环境变量配置总结

在 `.env` 文件中可以配置以下环境变量：

```bash
# HuggingFace 镜像站点（对 XetHub 无效）
HF_ENDPOINT=https://hf-mirror.com

# 禁用 XetHub 存储（推荐）
HF_HUB_DISABLE_XET=1

# 禁用 hf_transfer
HF_HUB_ENABLE_HF_TRANSFER=0

# 代理配置（如果需要）
HTTP_PROXY=http://your_proxy:port
HTTPS_PROXY=http://your_proxy:port
```

## 代码中的配置位置

在 `main.py` 中，应该在 `load_dotenv()` 之后设置环境变量：

```python
_ = load_dotenv()

# 设置 HuggingFace 相关环境变量（必须在工具初始化之前）
if not os.getenv("HF_HUB_DISABLE_XET"):
    os.environ["HF_HUB_DISABLE_XET"] = "1"  # 禁用 XetHub
if not os.getenv("HF_ENDPOINT"):
    os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"  # 设置镜像（对 XetHub 无效，但可能对其他模型有用）
```

## 相关链接

- [HuggingFace XetHub 博客](https://huggingface.co/blog/zh/xethub-joins-hf)
- [GitHub Issue: HF_ENDPOINT 不覆盖所有服务](https://github.com/huggingface/huggingface_hub/issues/2152)
- [HuggingFace Hub 文档](https://huggingface.co/docs/huggingface_hub)

## 问题状态

- ✅ 已识别问题：XetHub 存储不受 `HF_ENDPOINT` 影响
- ✅ 已找到解决方案：禁用 XetHub 或手动下载
- ⚠️ 待测试：`HF_HUB_DISABLE_XET` 环境变量是否在当前版本中生效

## 更新记录

- 2025-12-31: 初始问题记录和解决方案整理

